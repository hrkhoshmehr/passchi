/**
 * تور نمونه برای کاربر تازه.
 *
 * مسئله‌ای که این ماژول حل می‌کند: کاربر تازه نمی‌داند خروجی چه شکلی است.
 * توصیف‌کردنش کار نمی‌کند — «خلاصهٔ کلاس با ذکر منبع» برای کسی که ندیده،
 * یک ادعای تبلیغاتی است مثل هر ادعای دیگر. تنها راهِ فهماندن، **نشان‌دادن**
 * است.
 *
 * پس یک جلسهٔ واقعی — همان `example.mp3`، پنجاه دقیقه درس اخلاق اسلامی —
 * یک بار از خط لولهٔ واقعی گذشته و خروجی‌اش اینجا ذخیره شده. کاربر تازه
 * دقیقاً همان چهار پیامی را می‌بیند که اگر خودش صوت می‌فرستاد می‌گرفت.
 *
 * دو قاعده که در پیاده‌سازی رعایت شده‌اند:
 *
 * **۱) هیچ‌چیز ساختگی نیست.** این خروجی از مدل واقعی روی صوت واقعی آمده و
 * نقل‌قول‌هایش از همان دروازهٔ `verifyQuote` گذشته‌اند. اگر نمونه را دستی
 * قشنگ کنیم، اولین جلسهٔ خودِ کاربر ناامیدکننده می‌شود — و آن بدترین لحظهٔ
 * ممکن برای ناامیدی است.
 *
 * **۲) مرحله‌به‌مرحله، نه یک‌جا.** ده پیام پشت‌سرهم پس از `/start` کاربر را
 * فراری می‌دهد. هر گام پشت یک دکمه است، پس کاربر خودش جلو می‌رود و هر لحظه
 * می‌تواند بایستد.
 */

import path from "node:path";
import { fileURLToPath } from "node:url";
import { InlineKeyboard } from "grammy";
import type { AnalysisReport } from "../analysis/schema.js";
import { toFaDigits } from "../util/time.js";
import sampleReport from "./demo/sample-report.json" with { type: "json" };

/**
 * جلسهٔ نمونه — خروجی واقعیِ ذخیره‌شده.
 *
 * `as unknown as` لازم است چون JSON تایپِ ساختاری ندارد؛ خودِ فایل از
 * `report_json` یک جلسهٔ واقعی درآمده، پس شکلش با اسکیما یکی است.
 */
export const SAMPLE_REPORT = sampleReport as unknown as AnalysisReport;

/** مدت صوت نمونه (میلی‌ثانیه) — همان ۵۰ دقیقهٔ `example.mp3`. */
export const SAMPLE_DURATION_MS = 3_001_025;

export const SAMPLE_COURSE = "اخلاق اسلامی";

/**
 * فایل‌های همراه نمونه.
 *
 * کنار خودِ ماژول می‌مانند نه در `data/`: بخشی از کدند و باید با مخزن
 * منتقل شوند، وگرنه روی سرور تازه تور نصفه می‌ماند.
 *
 * ⚠️ مسیرها نسبت به **همین فایل**اند. اجرای تولید با tsx مستقیم روی `src`
 * است پس درست کار می‌کند؛ اگر روزی خروجی `tsc` در `dist` اجرا شد، این سه
 * فایل باید در مرحلهٔ build کپی شوند چون tsc فقط `.ts` را می‌برد.
 */
const here = path.dirname(fileURLToPath(import.meta.url));
export const SAMPLE_TRANSCRIPT_PATH = path.join(here, "demo", "sample-transcript.txt");
export const SAMPLE_PDF_PATH = path.join(here, "demo", "sample-notes.pdf");

export const DEMO_CB = {
  recap: "demo:recap",
  extracted: "demo:extracted",
  timeline: "demo:timeline",
  outro: "demo:outro",
} as const;

/** سرصفحهٔ گام اول — تا کاربر بداند این خروجیِ *نمونه* است نه جلسهٔ خودش. */
export const DEMO_INTRO =
  "🎬 <b>نمونهٔ یک کلاس واقعی</b>\n" +
  `<i>یه جلسهٔ ${toFaDigits(50)} دقیقه‌ایِ «${SAMPLE_COURSE}» رو براش پیاده کردم. ` +
  "این دقیقاً همون چیزیه که تو هم تحویل می‌گیری 👇</i>";

export function stepKeyboard(next: string, label: string): InlineKeyboard {
  return new InlineKeyboard().text(label, next);
}

/**
 * پیام پایانیِ تور.
 *
 * سه کار می‌کند و ترتیبش عمدی است: اول می‌گوید بالا نمونه بود (وگرنه کاربر
 * فکر می‌کند جلسهٔ خودش پردازش شده)، بعد دعوت می‌کند صوت خودش را بفرستد، و
 * آخر راه ارتباط با پشتیبانی را می‌دهد.
 *
 * دربارهٔ سهمیهٔ رایگان **دقیق** حرف می‌زند: آن ۱۵ دقیقه برای سنجیدن
 * «دقت صوت به متن» است، نه یک جلسهٔ کامل رایگان. کاربری که این را نداند و
 * انتظار جزوهٔ کامل داشته باشد، ناامید می‌شود — و انتظارِ اشتباه را خودمان
 * ساخته‌ایم.
 */
export function outroMessage(freeMinutes: number, supportUsername: string): string {
  const out = [
    "☝️ <b>چیزایی که بالا دیدی، خروجیِ صوت یه کلاس واقعی بود.</b>",
    "",
    "<b>حالا نوبت خودته.</b> صوت کلاستو بفرست تا همینا رو برات دربیارم.",
    "",
    `🎁 <b>یه صوت تا ${toFaDigits(freeMinutes)} دقیقه رایگان برات پیاده می‌کنم</b> — ` +
      "تا با گوش خودت بسنجی چقدر دقیق می‌شنوم. اصطلاح‌های تخصصی درس خودت، لهجهٔ استادت، " +
      "کیفیت ضبط گوشیت؛ اینا رو فقط روی صوت خودت می‌شه فهمید.",
    "",
    "<i>اون ۱۵ دقیقه فقط متنِ کلاسه. خلاصه و نکته‌های امتحانی و جزوه — همونایی که بالا دیدی — سکه می‌خواد.</i>",
  ];
  if (supportUsername) {
    out.push("", `❓ سوالی داشتی، به @${supportUsername} پیام بده.`);
  }
  return out.join("\n");
}
