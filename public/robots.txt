# فقط یک صفحهٔ عمومی داریم: لندینگ‌پیج. بقیه یا API است یا پوستهٔ مینی‌اپ.
User-agent: *
Disallow: /api/
Allow: /

# ⚠️ `/app` عمداً اینجا بسته **نشده**. اگر خزیدنش را ببندیم، گوگل باز هم
# ممکن است خودِ آدرس را بدون محتوا ایندکس کند و راهی برای گفتنِ «ایندکس نکن»
# نمی‌ماند. پس اجازهٔ خزیدن می‌دهیم و `noindex` را داخل `app.html` می‌گذاریم —
# که برای دیده‌شدن، باید خوانده شود.

Sitemap: https://passchi.ir/sitemap.xml
